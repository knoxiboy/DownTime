{
  "comment": "This root vercel.json is intentionally minimal. Deploy each service separately from its own subdirectory.",
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/.next",
  "framework": "nextjs",
  "installCommand": "cd frontend && npm install"
}
